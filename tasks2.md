Task 1 [DONE]
We want to be able to choose whether to talk to the controller or directly to claude code via a simple dropdown (like the model dropdowns)
This is so that we could interfere any time as a user and talk directly to claude code so that we could just add quick fixes if needed (the default agent to talk to would be the controller of course / so codex just like now)
When we talk to claude code directly, it would resume the same session so we could literally influence its session (the controller already resumes the claude code session every time it launches it, so not much of a change here, just a note that this would apply to manual user messages too)
> Completed: Added a persisted chat-target dropdown in the VS Code UI, routed plain-text input either to the controller or directly to Claude, reused the active Claude session for direct messages, and added focused routing tests.

Task 2 [DONE]
We want to add support for changing the controller cli to claude code as well instead of using codex. The default one would be codex, but we should have one more dropdown on the chat input box to choose what cli to use as the controller (the worker would always be claude code for now just like now)
You can already see how the repository uses claude code for the worker part so you will see how to pass it arguments / how to do real time streaming from it (JSON streaming) so you have everything to be able to add the support for claude code being the controller agent too. But this should be optional, the default controller agent should still be codex just like now. But if we change the dropdown on the input box to use claude code as the controller too, from that point it should use claude code (maybe this also means that it resets the chat session since obviously codex chat sessions are not compatible with claude code chat sessions but whatever, it is what it is. It's not really an issue)
> Completed: Added a persisted controller-CLI dropdown with Codex default and Claude optional, introduced a Claude-backed controller runner with controller-labeled streaming, reset incompatible controller session/model settings on CLI switch, and added focused controller CLI tests including no-Codex Claude-controller runs.
